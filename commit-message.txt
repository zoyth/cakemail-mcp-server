feat: implement comprehensive filter parameter support for logs API

Fixes all filter parameter compliance issues across the entire logs API surface area
to match Cakemail API specification requirements.

## Major Changes

### API Implementation (src/api/logs-api.ts)
- Add filter validation configurations for all endpoints with endpoint-specific rules
- Add `filter` parameter support to all log methods with validation
- Add missing `cursor` parameter to getCampaignLogs method
- Implement missing `getListLogs` method for /logs/lists/{list_id} endpoint
- Add comprehensive `validateFilter` method with detailed error reporting
- Make `log_type` parameter handling backward compatible for email logs
- Support proper filter syntax: `term==value;term2==value2`

### Valid Filter Terms by Endpoint
- **Campaigns**: additional_info, link_id, contact_id, email, uniques, group_by_contact, log_id, totals, type
- **Workflows**: additional_info, link_id, contact_id, email, log_id, track_id, type, group_by_contact  
- **Emails**: group_id, email, email_id
- **Lists**: additional_info, contact_id, email, uniques, group_by_contact, track_id, log_id, start_id, end_id, totals, type

### Main API Integration (src/cakemail-api.ts)
- Add getListLogs method proxy for new endpoint

### MCP Server Updates (src/index.ts)
- Add filter parameter to all existing log tool schemas
- Add new `cakemail_get_list_logs` tool for list logs endpoint
- Update all log tool handlers to support new filter parameters
- Add missing cursor parameter to campaign logs tool
- Make log_type parameter optional for backward compatibility in email logs
- Enhance response formatting to display filter information

## Compliance Status

✅ **BEFORE**: Multiple non-compliance issues
- Missing filter parameters across all endpoints
- Missing /logs/lists/{list_id} endpoint entirely  
- Missing required/optional parameters (cursor, log_type)
- No filter validation

✅ **AFTER**: Full API specification compliance
- Complete filter parameter support with validation
- All missing endpoints implemented
- All required parameters supported
- Comprehensive error handling and validation

## Breaking Changes

- `getTransactionalEmailLogs` now warns if `log_type` parameter is missing (previously ignored)
- Filter validation will log warnings for invalid syntax but continues gracefully

## New Features

- Filter validation with endpoint-specific rules
- New `/logs/lists/{list_id}` endpoint support
- Cursor-based pagination for campaign logs
- Comprehensive filter syntax support
- Backward compatibility for existing `type` parameter usage

Closes: filter implementation compliance issues
Related: Cakemail API specification validation
